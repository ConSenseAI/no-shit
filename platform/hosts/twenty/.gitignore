# Generated locally by setup.sh — never commit secrets or runtime state.
.env
__pycache__/
*.pyc
*.log
# Twenty's durable data (Postgres, attachments, CSV captures) lives OUTSIDE the
# repo at /home/user/fixture-runtime/twenty/ (durable disk); nothing runtime is
# written under this directory.
