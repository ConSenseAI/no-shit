# Open SaaS bench leg - Stripe test mode only.
# Copy to .env, chmod 600, and fill only the key. setup/demo populate
# ephemeral product, price, and webhook values in shell/container memory.
STRIPE_API_KEY=rk_test_REPLACE_ME
