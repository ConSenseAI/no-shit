# SPDX-License-Identifier: CC-BY-4.0 OR Apache-2.0
# SPDX-FileCopyrightText: 2026 Consense
id: no-dark-patterns
version: 0.1.7
status: draft                      # draft | candidate | active | deprecated
title: No Dark Patterns
supersedes: null

guarantee: >
  The interfaces through which you choose — buying, consenting, declining —
  show you real information and honest controls. No fabricated countdowns,
  stock counts, or activity feeds; no shame-worded or trick-worded choices;
  no hidden or disguised options; declining is as easy as accepting; nothing
  is added to your order or account without your explicit choice; and a "no"
  is remembered, not worn down.

harm_model: >
  Manipulation at the decision moment; a credence good whose deferral runs
  deeper than most: the manipulated interface looks BETTER at decision time
  (urgent deal, popular product, one-click consent). Harm arrives on the bill
  (unchosen add-ons, unlabeled charges), in the inbox and data trail
  (consents extracted via defaults, asymmetry, inverted wording), or never
  becomes visible at all — the counterfactual choice is invisible to its own
  victim, which is why market feedback cannot price it. Falls hardest on the
  hurried, the elderly, the less digitally literate, and second-language
  users.

scope:
  covers:
    - "truthfulness of pressure signals on choice surfaces: urgency (countdowns, deadlines), scarcity (stock/demand claims), social proof (activity feeds, testimonials, aggregates)"
    - "wording, semantics, and visual presentation of accept/decline controls; consent and permission prompts; defaults, pre-selections, decline persistence, and consent withdrawal"
    - "order/account integrity: what is added versus what was affirmatively chosen"
    - "recurrence of declined prompts"
    - "product-originated notifications (email/push/in-app) that present offers or choices"
  non_goals:
    - "cancellation flows and the auto-renewal disclosure/consent facts of signup -> no-subscription-trap (owner; reciprocal exclusion)"
    - "price honesty and completeness (drip pricing, hidden fees, price framing) -> transparent-pricing"
    - "substance of data practices (collection, sharing, sale, retention) -> no-surveillance (future); the consent INTERFACE is owned here"
    - "data export and account deletion (including deletion-flow obstruction) -> no-lock-in"
    - "advertising/content claims beyond the enumerated interface pressure signals (deception law's domain, not choice architecture)"
    - "engagement-maximizing design as such (infinite scroll, autoplay, streaks) — needs its own harm model; candidate future criterion"
    - "forced enrollment beyond bundled optional consents (e.g., account-required-to-purchase); the bundled-consent instance is check choice-symmetry's"
    - "comparison prevention -> transparent-pricing"
  related:
    - { id: no-subscription-trap, relation: overlaps }   # they own the cancellation flow + the auto-renewal disclosure/consent FACTS of signup (fact-based boundary, both specs identical as of their 0.1.6); everything else in signup is this criterion's
    - { id: no-surveillance, relation: overlaps }        # future: this criterion owns consent-INTERFACE manipulation; no-surveillance owns data-practice substance and MUST exclude the interface when authored
    - { id: no-lock-in, relation: overlaps }             # it OWNS obstruction within export/deletion flows (its check 5); this criterion excludes those flows (both sides identical as of no-lock-in 0.1.0)
    - { id: transparent-pricing, relation: complements } # MUST exclude was-the-addition-chosen facts (check no-unconsented-additions') when authored

observation_modes: [ code, behavioral ]   # union over checks; every check is either-mode (SPEC §4)

# Applicability: no covered choice surfaces at all (no purchase, consent,
# offer, or selectable-option surface) => every blocking check `na` =>
# INDETERMINATE, never a vacuous PASS (CRITERION-SPEC §4.4; SPEC §3).
#
# Mode-join rule (both modes run in one audit) — a total lattice, not a vote
# (CRITERION-SPEC 0.6.0 §4.7, adopted from this criterion's T-11; SPEC §4/§11):
#   decided outcomes join worst-of: fail > conditional > pass (evidence union,
#   never exoneration); a check decided in ANY mode is decided — unobserved in
#   the other mode never downgrades it; unobserved survives only if every mode
#   that ran returned it; na joins only with na. Confidence: per-check
#   evidence_completeness = max over modes run.

checks:
  - id: no-false-urgency-or-scarcity
    statement: >
      Covered surfaces MUST NOT display urgency or scarcity signals that are
      false: countdowns/deadlines not tied to a real expiry (reset, persist
      past zero, or gate nothing), "limited time" claims with no real limit,
      stock/availability/demand claims not backed by the real state they
      purport to report, or a technically-real signal deployed so the urgency
      communicated about the material decision is false.
    keyword: MUST_NOT
    modes: [ either ]
    evidence: [ source_ref, dom_snapshot, screenshot, flow_transcript, har_capture, notification_record ]
    procedure: >
      Inventory urgency/scarcity signals on covered surfaces, including
      captured product-originated notifications. CODE: trace each displayed
      value to its source (server-supplied state vs client-side generation —
      RNG, constants, per-load seeds). BEHAVIORAL: revisit across
      sessions/identities and past the stated expiry; record whether the timer
      resets, the offer survives its own deadline, or counts regenerate.
    decision: >
      ordered cascade, first match wins (§4.6):
      na if no urgency or scarcity signals on any covered surface;
      unobserved if signals present but the falsification procedure could not
      complete within the window (no post-expiry/cross-session observation
      possible and no code access);
      fail if any displayed signal is fabricated — timer resets or its expiry
      changes nothing; claimed limit does not exist; a count is generated
      client-side or by rule with no backing state; or a real signal about an
      immaterial element fabricates urgency about the material one;
      conditional if every signal is literally true but engineered to mislead
      at the margin (a real expiring timer whose equivalent offer relaunches
      promptly or on a recurring cadence — an overnight gap does not break
      seriality; serial flash sales — P-2, v0.1.3);
      pass otherwise (every displayed signal backed by real state, or honest
      signals only).
    severity: blocking

  - id: no-fabricated-social-proof
    statement: >
      Covered surfaces MUST NOT display social-proof signals that are
      fabricated or that misrepresent the data behind them: activity
      notifications generated rather than drawn from real events; testimonials
      or reviews that do not exist or were not made by users; or displayed
      aggregates curated to misrepresent the underlying data, including
      suppressing negative entries while presenting the remainder as the whole.
    keyword: MUST_NOT
    modes: [ either ]
    evidence: [ source_ref, har_capture, dom_snapshot, flow_transcript, screenshot, notification_record ]
    procedure: >
      Inventory social-proof elements, including those in captured
      product-originated notifications. CODE: trace displayed
      events/aggregates to their source; flag client-side generation,
      filtered aggregation, and operator-side configuration of third-party
      display thresholds. BEHAVIORAL: compare feeds across fresh identities
      and sessions; cross-check displayed aggregates against enumerable
      detail (review counts vs listed reviews).
    decision: >
      ordered cascade, first match wins; fail precedes unobserved — a
      permitted §4.6 deviation: suppression/mismatch shapes are decidable
      from captured evidence even when full verification is not:
      na if no social-proof elements on covered surfaces;
      fail if a signal is fabricated (generated events, invented testimonials)
      or a displayed aggregate misrepresents the underlying data (negatives
      suppressed from an aggregate presented as complete);
      unobserved if elements present but not falsifiable within the window
      (no source tracing or cross-session comparison possible), OR the
      aggregate's composition is operator-controlled (configurable
      third-party widget with display thresholds) and completeness cannot be
      verified in any mode run — unverifiable curated aggregates are partial
      scope, never a silent pass (plain uncontrolled third-party embeds are
      not, by themselves, unverifiable);
      conditional if signals are drawn from real data but framed to mislead at
      the margin (real events presented with false recency — days-old orders
      replayed as "just bought");
      pass otherwise (social proof traces to real events/users; aggregates
      reflect the underlying data).
    severity: blocking

  - id: no-confirmshaming
    statement: >
      On covered surfaces, declining an offer, enrollment, or consent MUST NOT
      require selecting a control worded or depicted to shame, belittle, or
      guilt the decliner — by self-disparagement, by editorializing the
      decline as harmful or foolish, or by shame-laden imagery attached to
      the decline path.
    keyword: MUST_NOT
    modes: [ either ]
    evidence: [ screenshot, dom_snapshot, source_ref, notification_record ]
    procedure: >
      Inventory offer/consent prompts on covered surfaces, including captured
      product-originated notifications; capture the exact wording and visual
      depiction of each decline control and its immediate framing.
    decision: >
      ordered cascade, first match wins (§4.6):
      na if no prompts with an accept/decline choice on covered surfaces;
      unobserved if such prompts exist but their decline controls could not be
      captured within the window;
      fail if any decline control is shame-worded or shame-depicted
      (self-disparaging; editorializing the decline as harmful/foolish/
      shameful; shame-laden imagery on the decline path);
      conditional if decline controls are neutrally worded and depicted but
      the prompt's surrounding copy or imagery leans on emotive or
      judgment-laden loss framing BEYOND factual consequence, while both
      options remain neutrally labeled;
      pass otherwise (neutral controls; consequence statements factual —
      a stated factual, specific consequence of declining is not
      confirmshaming and does not trigger the conditional band).
    severity: blocking

  - id: no-trick-wording
    statement: >
      Choice controls on covered surfaces MUST NOT carry wording or semantics
      that invert or materially misstate their effect: double negatives,
      label polarity reversed relative to effect, mixed opt-in/opt-out
      semantics within one form engineered to confuse, inconsistent mappings
      of the same control to different effects across adjacent contexts, or
      action controls whose label materially misstates what they do —
      including controls that place a charge or commitment without saying so.
    keyword: MUST_NOT
    modes: [ either ]
    evidence: [ dom_snapshot, source_ref, screenshot, flow_transcript ]
    procedure: >
      Inventory choice controls on covered surfaces; compare each label's
      plain-language meaning against the control's actual effect (CODE: the
      handler; BEHAVIORAL: exercise it and observe). Flag negation stacking,
      reversed polarity, mixed semantics, label-effect mismatches.
    decision: >
      ordered cascade, first match wins (§4.6):
      na if no choice controls on covered surfaces;
      unobserved if controls exist but their effects could not be established
      within the window (not exercisable, no code access);
      fail if any control's wording or semantics invert or materially misstate
      its effect, including (non-exhaustive; the umbrella predicate governs):
      a double negative or reversed polarity governing any covered choice
      control; adjacent controls mixing opt-in and opt-out semantics such
      that a consistent reading produces the wrong result; the same control
      mapped to materially different effects in adjacent contexts; or an
      action control effecting a charge/commitment its label does not state;
      pass otherwise (labels state effects plainly; polarity and mappings
      consistent).
    severity: blocking
    # Boundary (P-1, v0.1.3): this check owns what the control SAYS; rendered
    # selection/progress state contradicting reality is no-visual-interference's
    # misrepresented-state shape — one owner per fact.

  - id: no-visual-interference
    statement: >
      On covered surfaces, the product MUST NOT use visual presentation to
      hide or disguise choices: a decline/close/opt-out option rendered
      effectively undiscoverable (contrast, size, or placement — concealment);
      interactive elements disguised as something else (ads styled as content
      or system UI, fake close buttons, simulated OS dialogs); or visual state
      that misrepresents what is selected.
    keyword: MUST_NOT
    modes: [ either ]
    evidence: [ screenshot, dom_snapshot, source_ref ]
    procedure: >
      For each covered surface with competing options, capture the rendered
      presentation; record discoverability of the disfavored option (contrast,
      size, position, occlusion) and whether any interactive element
      masquerades as content or system UI. CODE corroborates via the styling
      rules that produce the rendering.
    decision: >
      ordered cascade, first match wins (§4.6):
      na if no covered surfaces presenting competing options or
      third-party/promotional elements;
      unobserved if such surfaces exist but rendered presentation could not be
      captured within the window;
      fail if an available option is concealed (near-invisible contrast/size,
      positioned where a user would not reasonably find it, or occluded); an
      interactive element is disguised as content or system UI; or displayed
      selection/progress state contradicts the actual state (a toggle
      rendered on while off; a plan highlighted as selected that was not
      chosen);
      conditional if the disfavored option is legible and discoverable in-flow
      but materially subordinated (small plain text-link against a large
      accent button) — visible, but styled to be overlooked;
      pass otherwise (competing options presented with ordinary emphasis
      differences at most — both legible, discoverable, presented as
      controls — and rendered state matches actual state).
    severity: blocking

  - id: choice-symmetry
    statement: >
      Where a covered surface asks the user to accept or decline something
      optional (a data/marketing consent, a permission, an optional add-on or
      enrollment; optional judged NORMATIVELY — a choice that could lawfully
      or functionally be declined while still obtaining the underlying
      product, regardless of how it is presented), declining MUST be
      available with parity: reachable in no more user actions than
      accepting, present on the same surface where acceptance is offered, and
      never pre-selected toward acceptance. Parity extends over the choice's
      lifecycle: a decline MUST be persisted no less durably than an
      acceptance, and withdrawing a granted consent MUST NOT require more
      actions than granting it did.
    keyword: MUST
    modes: [ either ]
    evidence: [ flow_transcript, step_count, dom_snapshot, screenshot, source_ref ]
    procedure: >
      Inventory optional accept/decline prompts on covered surfaces (optional
      judged normatively). For each: count minimum user actions to complete
      acceptance and declination; record whether decline is present on the
      first surface or requires navigation; record default states of
      consent/add-on controls. Record how each choice is persisted (CODE:
      cookie/flag TTLs and re-trigger conditions for accepted vs declined
      states). Where a consent was granted, exercise withdrawal and count its
      actions against the grant's.
    decision: >
      ordered cascade, first match wins (§4.6):
      na if no normatively optional accept/decline prompts on covered
      surfaces;
      unobserved if prompts exist but the decline path could not be exercised
      or counted within the window (e.g., variant-gated);
      fail if declining requires more actions than accepting (including
      exactly one more — reject behind an expander or second layer when accept
      is one action); or decline is absent from the surface where acceptance
      is offered (a consent wall for a normatively optional purpose is this
      shape); or the optional choice is pre-selected toward acceptance; or
      the declined state is persisted less durably than the accepted state
      (decline honored but re-asked while acceptances persist — TTL/flag
      asymmetry); or withdrawal of a granted consent requires more actions
      than the grant did;
      pass otherwise (decline available in actions <= accept, same surface,
      defaults neutral or off, declines persisted with parity, withdrawal no
      harder than grant).
    severity: blocking
    # Deliberately strict, no conditional band (SPEC §4 check 6, §12):
    # CCPA §7004 symmetry / CNIL decisions; loosening is free while draft
    # (T-4 window) if calibration demands. Lifecycle prongs (v0.1.1):
    # persistence parity = structural, code-decidable (pairs with check 8,
    # which owns re-prompt frequency); withdrawal parity = GDPR Art. 7(3)
    # made countable — consent WITHDRAWAL owned here; subscription
    # cancellation stays no-subscription-trap's, account deletion
    # no-lock-in's. Structural facts only — visual styling of the same
    # prompt is check 5's; wording is checks 3/4's. The auto-renewal consent
    # control at signup is no-subscription-trap check 5's; a marketing
    # checkbox on the same screen is this check's.

  - id: no-unconsented-additions
    statement: >
      The product MUST NOT add items, services, enrollments, or charges to a
      user's order or account without an affirmative choice: no sneaking items
      into a cart; no bundling an unrelated enrollment silently into another
      action; no charging an interaction that was not presented as a purchase
      or commitment; and no charge-committing control engineered for
      inadvertent activation (adjacency to unrelated controls, layout shift
      under the pointer, single-press charging from a browsing or loading
      context — with no confirmation step).
    keyword: MUST_NOT
    modes: [ either ]
    evidence: [ flow_transcript, screenshot, dom_snapshot, notification_record, source_ref, har_capture ]
    procedure: >
      Exercise the purchase/enrollment flow selecting only explicit choices;
      diff the resulting order/account state against the user's affirmative
      selections. CODE: inspect cart-mutation and enrollment triggers for
      paths not gated on an explicit user choice.
    decision: >
      ordered cascade, first match wins (§4.6):
      na if no order, enrollment, or charge-capable flows on covered surfaces;
      unobserved if flows exist but order/account state could not be reached
      or read within the window (e.g., payment stage not exercisable);
      fail if anything appears in the order or account the user never
      affirmatively selected — a silently added line item, an enrollment
      bundled unstated into an unrelated action, a charge triggered by an
      interaction not presented as a purchase/commitment, or a
      charge-committing control engineered for inadvertent activation
      (adjacency/layout-shift/single-press-from-browsing, no confirmation),
      whether or not a mis-hit was observed in the window;
      conditional if the addition was disclosed on the committing surface but
      not separately choosable (a stated extra bundled into a single
      "Continue" with no independent opt-out control);
      pass otherwise (order and account contain exactly what was affirmatively
      chosen; optional extras offered unselected).
    severity: blocking
    # Boundary: the amount/honesty of a disclosed price is transparent-pricing's;
    # a pre-selected add-on PRESENTED as a choice is check 6's; an addition
    # never presented as a choice is this check's; recurring-billing consent
    # is no-subscription-trap check 5's.

  - id: no-nagging-after-decline
    statement: >
      A prompt the user has declined (rating request, notification/permission
      ask, upsell, install banner) SHOULD NOT be re-presented within the same
      session, re-presented so often across sessions that it functions as
      wearing the user down, or re-presented so as to block task progress.
    keyword: SHOULD_NOT
    modes: [ either ]
    evidence: [ flow_transcript, screenshot, source_ref ]
    procedure: >
      Decline each recurring-prompt type once; continue using the product
      across the observation window; record every re-presentation of a
      declined prompt (same-session, cross-session counts, task-blocking).
      CODE corroborates via persistence and re-trigger logic.
    decision: >
      ordered cascade, first match wins (§4.6):
      na if no declinable recurring prompts on covered surfaces, or none were
      declined during observation;
      unobserved if prompts were declined but the window was too short to
      observe recurrence;
      fail if a declined prompt re-presents within the same session; or a
      declined prompt type re-presents three or more times across the
      observation window; or total re-presentations of declined prompts
      across all types reach four or more in the window (the type-carousel
      shape); or a re-presentation blocks task progress until re-answered;
      conditional if a declined prompt type re-presents exactly twice across
      the window and the cross-type total stays under four (persistent, at
      the tolerance edge);
      pass otherwise (declined prompts stay dismissed for the session and
      reappear at most once each, fewer than four total, across the window).
    severity: caveat
    # Counting unit is the prompt TYPE; counts denominated per 14-day window
    # (normalize pro rata; re-fixes when the probe script standardizes).
    # Mode note: CODE decides same-session and task-blocking arms
    # structurally; per-window count arms are behavioral. Thresholds are
    # first-draft values, queued for calibration (SPEC §12). As caveat
    # severity, fail here contributes CONDITIONAL, never FAIL.

aggregation:                       # CRITERION-SPEC §4.4 defaults, checks 1–7 blocking, check 8 caveat
  pass_if:          "all blocking checks (1-7) pass (na excluded, and at least one blocking check applied — §4.4); no check conditional; check 8 pass, na, or unobserved"
  fail_if:          "any blocking check (1-7) fails (enumerate ALL failing blocking checks — §5.2)"
  conditional_if:   "no blocking fail AND (any check returns conditional OR check 8 (caveat) fails OR a blocking check is unobserved / partial scope)"
  indeterminate_if: "audit could not run or reach consensus at all; or every blocking check returned na — no covered choice surfaces, criterion did not apply (§4.4)"
  # check 8 unobserved does not downgrade (non-blocking partial scope — §4.4)

verdicts: [ PASS, CONDITIONAL, FAIL, INDETERMINATE ]

confidence:                        # §5.3
  method: >
    min over contributing checks of (model_agreement x evidence_completeness),
    where evidence_completeness reflects how much of each check's
    falsification procedure completed in the mode(s) run; per check,
    completeness is the max over modes that ran (a second mode never reduces
    it — the join analogue of the mode-join lattice).
  bands: { high: ">=0.90", medium: ">=0.70 and <0.90", low: "<0.70 -> INDETERMINATE + review" }

corpus:                            # §6
  public: ./corpus/public/
  sealed_ref: null                 # hash-committed on the path to candidate/active (§6.2)
  minimums: { clean: 10, violating: 10, boundary: 5, adversarial: 5, injection_canary: 3, defeat_device: 2 }

validation:                        # §7 — nulls until measured; gates status transitions
  human_calibration:               # §7.1 — target pre-registered at v0.1.4, before any human adjudication
    target: { aggregate: "Cohen's kappa >= 0.8", hard_subset: ">=80% exact-verdict on boundary+adversarial pooled", registered_at: 2026-07-06 }
    adjudicators: 0
    agreement_aggregate: null
    agreement_hard_subset: null    # boundary+adversarial pooled
    corpus_covered: null           # fraction of public-corpus rows adjudicated (SHOULD be 1.0)
  frontier_pool:                   # per (pool x mode artifact) — CRITERION-SPEC 0.6.0 §7.2 (T-10)
    code:       { false_pass: null, false_flag: null, indeterminate: null, agreement: null, cost_usd: null }
    behavioral: { false_pass: null, false_flag: null, indeterminate: null, agreement: null, cost_usd: null }
  enclave_pool:                    # APPLIES here (dual-mode, code-audit keystone) — not n/a, unlike the purely behavioral sibling (§7.2)
    code:       { false_pass: null, false_flag: null, indeterminate: null, agreement: null, cost_usd: null }
    behavioral: { false_pass: null, false_flag: null, indeterminate: null, agreement: null, cost_usd: null }
  kill_thresholds: { false_pass_max: 0.05, false_flag_max: 0.10 }   # illustrative until the corpus is assembled (§7.2); sealed set sized >= 3/epsilon per side

claim_template: >                  # §8
  "Artifact {subject} satisfied no-dark-patterns@{version} under rubric
   {rubric_version}, audit artifact {prompt_version}, model set {model_set},
   modes {modes}; measured false-pass {fp} / false-flag {ff} on corpus
   {corpus_hash}."

prompt_bundle:                     # §9 (CRITERION-SPEC 0.6.0, T-10): one audit-artifact reference per declared mode; prompt_version identifies the (mode, version) pair(s) actually run
  code:       { ref: null, version: null }   # planned: over-source prompt bundle
  behavioral: { ref: null, version: null }   # planned: probe script

provenance:                        # §10
  authors: [ "Scott Helvick" ]
  reviewers: [ ]
  sources:
    - "Brignull — deceptive.design dark-pattern taxonomy"
    - "Mathur et al., Dark Patterns at Scale (2019)"
    - "Gray et al., The Dark (Patterns) Side of UX Design (CHI 2018)"
    - "FTC Staff Report, Bringing Dark Patterns to Light (2022)"
    - "OECD, Dark Commercial Patterns (2022)"
    - "In the Matter of Epic Games, Inc. (FTC, 2022-2023) — public record"
    - "In the Matter of Fashion Nova, LLC (FTC, 2022) — public record"
    - "CNIL SAN-2021-023 (Google) / SAN-2021-024 (Facebook), deliberations 31 Dec 2021, announced Jan 2022 — public record"
    - "European Commission / CPC — Booking.com commitments (announced Dec 2019, deadline June 2020, confirmed Dec 2020); CPC dark-pattern sweep (2023)"
    - "GDPR Art. 7(3) — withdrawal-parity anchor (check choice-symmetry)"
    - "EDPB Cookie Banner Taskforce report (2023)"
    - "CCPA regulations, 11 CCR §7004 (symmetry in choice)"
    - "EU Digital Services Act Art. 25; UCPD guidance (2021/C 526/01)"
    - "UIGuard (Chen et al., 2023); DarkBench (2025)"
  first_active: null
  delegation_inclusion_count: null
  overturn_rate: null
  changelog:
    - { version: 0.1.7, date: 2026-07-06, change: "cross-vendor panel run 2 (Claude + GPT-5, full 46-row corpus, blinded packets): 46/46 kappa 1.00, both 46/46 vs key; check-6 strictness and check-5 middle band held on an unrelated lineage. No check, decision, or corpus changes. Conforms-to 0.6.1 (T-12, additive). Evidence: calibration/panel-2026-07-06/", rationale: "strict positions now have cross-lineage rule-over-instinct evidence; watch-items stand for the human gate; human_calibration stays 0" }
    - { version: 0.1.6, date: 2026-07-06, change: "corpus README status prose synced (dry-run recorded at 0.1.3, 7.1 target registered at 0.1.4 — the README still described both as pending); no row, check, decision, or label changes", rationale: "public record consistency; the registration and dry-run commits touched SPEC and manifest but missed the corpus README's status narration" }
    - { version: 0.1.5, date: 2026-07-06, change: "editorial: no-lock-in authored — relation re-tagged overlaps (it owns exit-flow obstruction; reciprocal now stated on both sides); unauthored-criteria framing narrowed to transparent-pricing/no-surveillance; no boundary changes", rationale: "cross-criterion ownership must be stated identically on both sides (CRITERION-SPEC 3.3); the when-authored framing became stale the moment no-lock-in landed" }
    - { version: 0.1.4, date: 2026-07-06, change: "7.1 human-calibration target pre-registered: Cohen's kappa >= 0.8 aggregate AND >=80% exact-verdict on boundary+adversarial pooled (registered_at 2026-07-06); no other changes", rationale: "stated-in-advance discipline (CRITERION-SPEC 7.1) — registered after the first dry-run, before any human adjudication; mirrors the sibling's target" }
    - { version: 0.1.3, date: 2026-07-06, change: "first calibration dry-run folded in (calibration-dryrun.md): blind Sonnet+Opus panel, 22 id-stripped scenarios — 22/22 inter-rater (kappa = 1.00), both raters 22/22 vs key, zero underdetermined. P-1: check-4/check-5 ownership line (control's wording vs rendered state). P-2: check-1 seriality wording (promptly-or-recurring-cadence; overnight gap does not break seriality). Section 12 rewritten to probed status; strict positions held under blindness with instinct divergences logged as the human gate's watch-list", rationale: "operationalization evidence at dry-run scale; the two panel findings were the only text gaps found; human_calibration stays 0 — LLM proxy is not the formal gate" }
    - { version: 0.1.2, date: 2026-07-06, change: "conformance migration to CRITERION-SPEC 0.6.0: T-10/T-11 now mold rules (9, 7.2, new 4.7); per-mode prompt_bundle and per-(pool x mode) validation cells are the legal Appendix A shapes; SPEC 11 marked resolved; no check, decision, boundary, or corpus changes", rationale: "the mold adopted this criterion's two findings (with the review's lattice + per-mode-validation refinements); interim framings replaced by mold citations" }
    - { version: 0.1.1, date: 2026-07-06, change: "review pass (blind domain/adversarial reviewer + inline conformance adjudication of all rows). Blocking T-8 fix: check no-visual-interference fail arm covers misrepresented selection state (+viol-syn-fake-selected-state). no-confirmshaming: conditional arm qualified to emotive/judgment-laden loss framing BEYOND factual consequence (+clean-syn-factual-consequence); shaming covers depiction (+adv-syn-emoji-shame). no-trick-wording: fail enumeration non-exhaustive + de-narrowed; inconsistent same-control mappings added (Epic row now literal). choice-symmetry: optional defined normatively (+viol-syn-consent-wall); lifecycle parity — decline persistence (+viol-syn-reject-resets) and consent withdrawal (+viol-syn-unsubscribe-maze, GDPR 7(3)). no-unconsented-additions: inadvertent-activation shape (Epic mis-hit lane). no-fabricated-social-proof: operator-controlled unverifiable aggregates -> unobserved; fail-before-unobserved (permitted 4.6 deviation). no-nagging-after-decline: cross-type aggregate arm >=4 (+bound-syn-prompt-carousel); 14-day denomination. Covered surfaces include product-originated notifications; checks 1-3 gain notification_record. Mode-join restated as total lattice (decided beats unobserved; worst-of decided; na joins na); T-10 extended to per-(pool x mode) validation cells; confidence completeness = max over modes. Ownership with no-subscription-trap restated fact-based (their 0.1.6); reciprocal MUST-excludes for unauthored criteria; no-surveillance relation -> overlaps; forced-enrollment + comparison-prevention non-goals. Corpus 39->46; CNIL dockets corrected (SAN-2021-023/-024); Booking dates precise; FTC captions In-the-Matter-of; guarantee tail softened", rationale: "one blocking statement<->decision gap (T-8), one internal contradiction on the highest-frequency clean shape, wrong enforcement dockets, and a mode-join edge where both-modes audits yielded weaker verdicts than single-mode — all verified against file text before fixing; cheap evasion lanes closed now (notifications, depiction, persistence, mis-hit engineering, type-carousel, unverifiable aggregates), the rest queued to SPEC §12; boundary moves permitted pre-active (T-4/T-7); every new shape carries a corpus anchor; none probed yet" }
    - { version: 0.1.0, date: 2026-07-06, change: "initial draft — eight checks, dual-mode (every check either); scope drawn against no-subscription-trap / transparent-pricing / no-surveillance / no-lock-in; strict check-6 opening position; mold findings T-10 (dual-artifact binding) and T-11 (mode-join rule) recorded", rationale: "first multi-mode criterion — the design doc's keystone test of code-audit judgment; authored to stress the mold's per-check modes machinery the way no-subscription-trap stressed its single-mode shape" }
